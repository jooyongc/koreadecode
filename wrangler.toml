name = "koreadecode-web"
pages_build_output_dir = "."
compatibility_date = "2024-01-29"

# Placement: Functions를 Google API 서버 근처(미국)에서 실행
# 한국에서 Gemini API "User location not supported" 에러 해결
[placement]
mode = "smart"
hostname = "generativelanguage.googleapis.com"

[vars]
# 환경 변수는 Cloudflare 대시보드에서 설정하는 것을 권장합니다.
